# pipewire-lhdc-aur

LHDC v5 support has been merged into the official PipeWire repository. The same upstream-consistent implementation is now also available directly from `pipewire/pipewire:master`.
